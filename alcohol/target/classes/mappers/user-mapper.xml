<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- UserMapper.xml -->
<mapper namespace="com.mid.alcohol.repository.UserRepository">
  
  <!-- 회원가입 (이메일) -->
  <insert id="userEmailSignup">
    INSERT INTO USERS (userEmail)
    VALUES (#{userEmail})
  </insert>
  
  <!-- 회원가입 정보  -->
  <insert id="userMainSignup" >
    insert into USERS (userEmail, userPassword, userPasswordCheck, userName, userAge, userAddress, userPhone, userNickname, userBirthday, userJoindate, userGender)
    values (#{userEmail}, #{userPassword}, #{userPasswordCheck}, #{userName}, #{userAge}, #{userAddress}, #{userPhone}, #{userNickname}, #{userBirthday}, #{userJoindate}, #{userGender})
     
  </insert>

  <!-- 이메일로 중복체크 -->
  <select id="findByUserEmail" resultType="com.mid.alcohol.domain.User">
    select * from USERS where userEmail = #{userEmail}
  </select>
  
  <!-- 별명으로 중복체크 -->
  <select id="findByUserNickname" resultType="com.mid.alcohol.domain.User">
    select * from USERS where userNickname = #{userNickname}
  </select>
</mapper>